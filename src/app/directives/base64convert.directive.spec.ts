import { Base64convertDirective } from './base64convert.directive';

describe('Base64convertDirective', () => {
  it('should create an instance', () => {
    const directive = new Base64convertDirective();
    expect(directive).toBeTruthy();
  });
});
